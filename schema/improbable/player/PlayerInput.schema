package improbable.player;

type Joystick {
    float x_axis = 1;
    float y_axis = 2;
}

type Fire {}

component PlayerInput {
    id = 1004;
    Joystick joystick = 1;
    // The component event for triggering firing.
  	event Fire fire;
}
